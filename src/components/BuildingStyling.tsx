import React from 'react'
import { IonButton, IonInput } from '@ionic/react'
import styled from 'styled-components'
import { Button } from '@material-ui/core'

const Julian = styled(IonButton)`
  position: fixed;
  margin-left: 15rem;
  margin-top: 7.5rem;
  background-color: green;
`

export { Julian }
